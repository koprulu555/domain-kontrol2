name: 'BaseURL Güncelleyici'
on:
  workflow_dispatch:
  schedule:
    - cron: '*/30 * * * *'

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Repoyu Çek
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 🐍 Python Kur
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: 📦 requests Yükle
        run: pip install requests

      - name: 🎯 BaseURL Çek
        run: python3 fetch_baseurl.py

      - name: 📝 Değişiklikleri Kaydet
        if: always()
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          
          if [ -f "guncel_baseurl.txt" ]; then
            if git diff --quiet guncel_baseurl.txt; then
              echo "ℹ️  Değişiklik yok"
            else
              git add guncel_baseurl.txt
              git commit -m "🔗 BaseURL güncellendi"
              git push
              echo "✅ Değişiklikler kaydedildi"
            fi
          else
            echo "⚠️  guncel_baseurl.txt oluşturulamadı"
          fi
